version: 1

deploy:
  steps:
    setupVariables:
      after:
        - chmod -f +x ./*.sh
        - whoami
        - python3 -m pip install --no-cache-dir -r requirements.txt # 2>&1 >/dev/null
        - pip3 show azure-keyvault-secrets
        - ./checkvars.sh
